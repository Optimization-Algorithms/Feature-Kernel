\begin{algorithm}[H]
    \caption{Build Initial Kernel}\label{algo:build-kernel}
    \begin{algorithmic}
        \STATE {$dataset = generate\_data\_set(solutions);$}    
        \STATE {$feature\_importance = generate\_feature\_importance(dataset); $}
    \end{algorithmic}
\end{algorithm}
